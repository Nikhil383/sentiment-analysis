version: "3.8"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./models:/app/models:ro
    environment:
      MODEL_PATH: "/app/models/hf_model"
      TORCH_DEVICE: "-1"
